export const typeDefs = `#graphql ->way of comment #
    type User {
        id: ID!
        firstName: String!
        lastName: String
        email: String!
        profileImageURL: String
    }
`;
