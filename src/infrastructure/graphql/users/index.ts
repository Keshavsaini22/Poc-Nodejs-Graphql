import { typeDefs } from "./users.typedefs";
import { queries } from "./users.queries";
import { mutations } from "./users.mutations";
import { resolvers } from "./user.resolvers";

export const User = { typeDefs, queries, mutations, resolvers };
